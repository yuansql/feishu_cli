# CONTEXT

飞书工作伙伴（自建 CLI）。身份与应用 ID 只在本机 `lark-cli` / 环境变量，不写进文档。

领域词：lark-cli、工作伙伴、user 身份办事、bot 身份收发、缺权限明示、不代登 OAuth。

边界：不克隆 aily；Hermes 禁止 --yolo；单聊走隔离档案 `feishupartner` + 白名单 `feishu mcp`（无终端）；群里只润色；仓内零密钥；飞书权威失败则不假装成功。Hermes SSL/断线原文不进飞书，回落已取材料。
监视：只看得见机器人所在群的开放平台事件。同事单聊后台仅 09:00–18:00 每小时拉一次（登录用户，不是 messages-search）；说「今天/明天任务」时也会当场拉做成【要跟的活】。群聊防遗忘走本地跟进账（截止日/对接人 B/自己转交/口语派活）。表格艾特可 3 分钟扫已配置的多维表。
换人部署：`DEPLOY.md`；身份用 `FEISHU_PARTNER_*` 环境变量，勿提交密钥。
aily：对齐日程/待办/文档/周报/纪要/审批，不克隆工作台与额度。问哪个群走会话搜索，不拿文档追问。问某人回复/怎么说走按人取会话，不搜文档。未知句默认不搜文档（要搜再说「搜」）；单聊未知会先分类再取数。单聊 `简报` / `今天` / `明天任务` 发 JSON 2.0 卡片（日程可点、已接受绿色），禁止只回纯文本把样卡格式丢掉。单聊「写文档 / 给我写个这个」按提纲 `docs +create` 新建云文档；贴现有链接说「写到…下面」走 ArtifactTask：持久化目标/区块/草稿，先展示草稿，「写进去」才 `docs +update` 原地修改并回读验真，「多一点」续改同一 block，「结束」关闭。聊天禁止甩 fetch 的 `<title>`/Markdown 原文。`今天的任务` 走今天卡片（日程+待办+跟进账），禁止搜文档。单聊记上一轮；「详细点」/序号接着问；「删除这个待办」走飞书 `task +complete`，权威失败不假装已删。回消息先表情 `OnIt` + 短句（「在看明天…」/「在查待办…」），再跑 lark-cli/Hermes；表情失败仍发短句，短句失败仍给正式答。发出去后验结果：瞬时失败重发一次；正文是 SSL/泄漏则改发材料。
任务规划：`feishu plan <目标>` 与单聊「规划/拆解…」走 TaskRunner：拉日程/待办/知识源→汇总→「确认写入」同步跟进账与飞书待办；「下一步」「任务进度」续跑。CLI `feishu plan` 仍只打印文本。不等同官方异步沙箱。差距见 `AILY_ALIGNMENT.md`。
每天 09:00：`feishu brief --push` + LaunchAgent `com.feishu.partner.brief`（不能少）；serve 同期是睡眠备份。两路共用 stamp **先占坑**，只发一张卡，禁止卡失败再叠文字造成双发。简报主通道是飞书 JSON 2.0 卡片：日程标题可点进原事件；待回复给出「去回复」（不放进会按钮）；群消息「去看」+「已处理」。工作日 09:00 另推「今日待跟进」卡（已完成/明天再说/忽略）。周一 09:05 `feishu followup --weekly-once` 写多维表「本周任务」并私聊清单；飞书建表失败则只发本地清单。卡片回调 RSVP 要等开放平台订 `card.action.trigger`，未订则点进日程里接受。不克隆今日/未结束筛选。收消息：`feishu serve --install` → `com.feishu.partner.serve`。待处理要分未回复/已追问未答完/已答完。单聊说「已处理/解决了/已经完成/搞定」销账：若回复伙伴发出的派活消息，先从合并 `content` 拆新话，再按回引中的人名 + 原任务正文精确销 `followups`（正文优先于人名，避免同人多条误中），回执复述具体任务；回引「待处理/待回复（N项）」则整批销简报 pending；过期/重复回引不动其他条。无回引的弱提示才列候选，禁止猜测、帮助提示或改走「某人最近怎么说」。09:00 可附卡片按钮（需开放平台订阅 `card.action.trigger`，未订则文字销账仍可用）。单聊「今日待跟进」「生成本周任务」。CLI：`feishu followup --setup-tables|--digest-once|--weekly-once|--scan-bitable`。
