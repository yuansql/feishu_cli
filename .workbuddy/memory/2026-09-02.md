# 2026-09-02

## 已完成
- 修复简报误报不相关消息：`_collect()` 中 `recent_items` 循环未做"关于我"过滤，导致 inbox 里别人的消息被当成待处理。
  - 在 `partner/office/brief.py` 第 945 行加入 `if not _about_user(item): continue`，复用已有 `_about_user()` 判定（被@ / 名字在文中）。
- 改造 assign-push 消息样式：从纯文本改为 interactive card 发送。
  - `followup.py` 新增 `_parse_card_text()` 从降级文本提取 (标题, 正文, 链接)。
  - `followup.py` 新增 `assign_push_card()` 生成卡片：灰色头 "📝 有人派活"、富文本正文、"查看详情" 跳转按钮、"完成" 回调按钮。
  - `serve.py` 的推送循环优先发卡片，fallback 到纯文本。
- 全量测试：426 tests，1 既有失败 + 16 网络错误（基线一致）；smoke 23/23 + 16/16 全绿。

## 备注
- 杨庆海 @ 侯帅臣/孙萌/师鹏飞/平高策/王永豪 的消息（"飞猫管家 3.2.501 已全部上线"）之前被 inbox 误判，简报列成了你的待处理。加过滤后不会再出现。
