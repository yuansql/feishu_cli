# 2026-08-26 工作记录

- 主题：将 feishu_cli 向飞书 aily 对齐的优化方向落地为可执行 PR 计划。
- 方法：使用 /adhd 多帧发散（competitor/markets/game-design/remove-assumption/speedrunner），收敛到 3 个核心差距：零摩擦卡片确认、群聊隐式上下文、多人共享运行时。
- 产出：AILY_ROADMAP_PRS.md，明确 PR1~PR4 的改动模块、数据模型、验收标准与风险缓解。
- 路线图顺序：先 PR1 卡片确认闸（体验差异最显性、风险最低），再 PR2 群聊上下文，最后 PR3 多人共享运行时；每个 PR 同步刷新 aily 对齐分。
- 代码落地 PR1：交互式确认卡片、`approvals` SQLite 状态机、`approve`/`decline` 回调处理、超时撤销心跳，现已通过 `bash scripts/check.sh`（370 tests OK + smoke 16/16）。
- 真实环境联调：在飞书机器人单聊成功下发确认卡片；手动 decline 链路（任务 cancelled + 结果卡片回执）跑通。飞书卡片按钮自动回调到 serve 未收到 payload，原因可能是开放平台事件订阅/权限/卡片 action 回调配置未开启，需在开放平台核对。
- 追加 PR1 文本回复 fallback：用户在飞书回复「确认写入」「取消」等关键词即可审批当前 pending approval，解决卡片按钮回调未到位时的可用性问题；新增 `tests/test_serve_approval_text.py`。
- 代码落地 PR2 骨架：新建 `partner/core/chat_context.py`（群聊元数据采集、24h TTL 衰减、按聊天读取）；`partner/ops/serve.py` 在收到用户消息时采集、每小时衰减；`partner/runtime/agent/service.py` 在 Agent v2 任务运行前注入最近上下文；`partner/ops/setup.py` 增加 `--ambient-context {on,off}`；`feishu memory --clear-chat-context` 可清空；新增 `tests/test_chat_context.py`。`bash scripts/check.sh` 379 tests OK + smoke 16/16。
- 代码落地 PR3（多人共享运行时 / intent-patch）：扩展 Agent v2 任务 schema（`schema_version=4`、`intent_patches`）。新增 `partner/runtime/agent/service.py` 的 `append_intent_patch`、`_merge_intent_patches`、`claim_confirmation`、`resume_agent_task_after_claim`；cancel patch 可直接终止任务；append/override patch 合并进 observations 供 Agent 重规划。`partner/ops/serve.py` 群聊 @bot 时，非控制指令自动追加为 intent patch；用户说「我来确认」可接管 blocked 任务的写权限；检测到 cancel+append/override 冲突时发送澄清卡片。新建 `partner/office/progress_card.py` 生成「任务进度简报」卡片；扩展 `feishu runs --patches --task-id` 查看补丁、`feishu runs --progress --task-id` 推送进度卡。`list_runs_text` 支持按 task_id 查询。新增 `tests/test_intent_patch.py`。`bash scripts/check.sh` 386 tests OK + smoke 16/16。已提交 commit `635d6ef`。
- 代码落地 PR4（Aily 对齐分刷新）：更新 `partner/ops/aily.py` 中 5 项能力分与 `current`/`gap` 描述：`agent_runtime` 84→92、`feishu_tools` 80→86、`governance` 48→66、`memory` 74→88、`multi_agent` 78→86，`alignment_score()` 从 77.4→80.8。补齐 `AILY_ROADMAP_PRS.md` PR2 验收状态。`bash scripts/check.sh` 386 tests OK + smoke 16/16。已提交 commit `b72c1f6`。
- 代码落地 PR6（条件触发器 + 统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook`，`condition` 含 keywords/chat_type/sender_id/webhook_path；统一 `trigger_event_log` 表按 `(source, external_id)` 幂等去重；`partner/ops/serve.py` 消息链路增加 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按 `/webhook/<path>` 优先匹配 webhook 触发器，fallback 旧版通用 webhook；扩展 `partner/ops/triggers_cli.py` 支持 `--source` 与 `--condition-*` 及 `feishu triggers log`。更新 `partner/ops/aily.py`：`triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py`。已提交 commit `9f933d2`。
- 综合测试 + bug 修复：运行 `bash scripts/check.sh` 417 tests OK + smoke 16/16；实测 `feishu triggers -- add/list/log`、`next_run` 边界、消息触发匹配、`feishu runs --patches`。发现并修复 `partner/runtime/agent/service.py` 缺失 `format_patches_text` 导致的 `feishu runs --patches --task-id` 报错。
- 代码落地 PR6（条件触发器与统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook`、condition（keywords/chat_type/sender_id/webhook_path）、统一 `trigger_event_log` 表；`partner/ops/serve.py` 消息链路新增 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按子路径匹配 webhook 触发器并 fallback 旧版通用队列；`partner/ops/triggers_cli.py` 支持 `--source` / `--condition-*` 与 `log` 子命令；`partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 81.5→82.0。新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 综合测试与 bug 修复：跑通 `scripts/check.sh` 417 tests OK；CLI 实测 `feishu triggers add/list/log` 三种 source；验证 `feishu runs --patches --task-id` 输出；边界测试 daily/weekly/cron/once next_run；实测消息触发关键词/聊天类型/发送者/bot 过滤。发现并修复 `partner/core/run_store.py` 引用不存在的 `partner.runtime.agent.service.format_patches_text` 导致 `feishu runs --patches` 崩溃的 bug，在该模块新增 `format_patches_text()`。
- 代码落地 PR6（条件触发器与统一事件日志）：扩展 `partner/core/triggers.py` 支持 `schedule/message/webhook` 三类触发器与 `condition` 匹配；新增统一 `trigger_event_log` 审计表，按 `(source, external_id)` 幂等去重；`partner/ops/serve.py` 处理消息时启动关键词触发任务；`partner/ops/webhook.py` 按路径匹配 webhook 触发器后 fallback 通用队列；扩展 `feishu triggers add --source ... --condition-*` 与 `feishu triggers log`；新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。更新 `partner/ops/aily.py`：`triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。已提交 commit `9f933d2`。
- 综合测试与 bug 修复：运行 `scripts/check.sh`、triggers CLI 实机（schedule/message/webhook add/list/log）、schedule 边界与消息触发匹配；发现 `run_store.list_runs_text` 引用不存在的 `format_patches_text`，已在 `partner/runtime/agent/service.py` 实现 `format_patches_text(task)` 函数，修复后 `feishu runs --patches --task-id <id>` 正常显示意图补丁。已重新跑 `bash scripts/check.sh` 全绿。
- 代码落地 PR6（条件触发器与统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 source=schedule/message/webhook 与 condition（keywords/chat_type/sender_id/webhook_path）；新增统一 `trigger_event_log` 表与幂等事件记录；`partner/ops/serve.py` 在消息处理链路调用 `_maybe_run_message_triggers` 自动启动 Agent 任务；`partner/ops/webhook.py` 按 `/webhook/<path>` 优先匹配 webhook 触发器；扩展 `feishu triggers log` 审计与 `--condition-*` 参数；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0；新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 测试跑测：完整运行 `bash scripts/check.sh` 417 tests OK + smoke 16/16；用隔离 DB 实测 `feishu triggers -- add/list/log`、schedule/once/weekly/cron 边界、`match_message_trigger` 关键词/聊天类型/发送者过滤、`feishu runs --patches --task-id` 输出。过程中发现并修复一个 bug：`partner/core/run_store.py` 引用 `partner.runtime.agent.service.format_patches_text` 但该函数不存在；补齐 `format_patches_text(task)` 后重跑全绿。
- 代码落地 PR6（条件触发器与统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook` 与 `condition`（keywords/chat_type/sender_id/webhook_path），新增统一 `trigger_event_log` 表；`partner/ops/serve.py` 处理消息时自动匹配 message 触发器；`partner/ops/webhook.py` 按子路径匹配 webhook 触发器并 fallback；扩展 `triggers_cli.py` 支持 `--source` / `--condition-*` / `log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py` 覆盖匹配、日志、CRUD 校验。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 综合测试 + bug 修复：运行 `scripts/check.sh`、triggers CLI 实测、schedule 边界、message trigger 匹配、runs --patches 实测。发现并修复 `partner/runtime/agent/service.py` 缺少 `format_patches_text` 导致的 `feishu runs --patches` 崩溃；函数返回任务状态/目标/补丁列表/observations。补测通过 417 tests OK。
- 代码落地 PR6（条件触发器与统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 source=schedule/message/webhook，condition 支持 keywords/chat_type/sender_id/webhook_path；新建 `trigger_event_log` 统一审计表与幂等去重；`partner/ops/serve.py` 消息流调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按子路径匹配 webhook 触发器并 fallback 旧版队列；扩展 `partner/ops/triggers_cli.py` 支持 `--source`/`--condition-*` 与 `log` 子命令；新增 `tests/test_triggers_conditions.py`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0；更新 `AILY_ROADMAP_PRS.md`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 跑测试时修复 bug：`partner/core/run_store.py` 的 `list_runs_text(task_id=...)` 引用了不存在的 `format_patches_text`，在 `partner/runtime/agent/service.py` 中补齐该函数，输出任务状态、目标、意图补丁列表与 observations。`bash scripts/check.sh` 复测 417 tests OK + smoke 16/16。
- 代码落地 PR6（条件触发器与事件日志审计）：扩展 `partner/core/triggers.py` 支持 source=schedule/message/webhook，`condition` 支持 keywords/chat_type/sender_id/webhook_path；新增统一审计表 `trigger_event_log`；`partner/ops/serve.py` 处理消息时调用 keyword 触发器；`partner/ops/webhook.py` 按 `/webhook/<path>` 匹配 webhook 触发器；扩展 `partner/ops/triggers_cli.py` 支持 `--source`/`--condition-*` 与 `feishu triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py` 覆盖匹配、日志、校验。已提交 commit `9f933d2`。
- 综合测试跑 bug 修复：在实测 `feishu runs --patches` 时发现 `partner/core/run_store.py` 引用了未定义的 `partner.runtime.agent.service.format_patches_text`，补写该函数后 `scripts/check.sh` 417 tests OK + smoke 16/16。已提交。
- 代码落地 PR6（条件触发器 + 统一事件日志）：扩展 `partner/core/triggers.py` 支持 source=schedule/message/webhook 与 condition（keywords/chat_type/sender_id/webhook_path）；新增统一 `trigger_event_log` 表与幂等审计；`partner/ops/serve.py` 收到消息时匹配 message 触发器，`partner/ops/webhook.py` 按子路径优先匹配 webhook 触发器并 fallback 旧版通用入口；扩展 `partner/ops/triggers_cli.py` 支持 `--source`/`--condition-*` 与 `feishu triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py` 覆盖匹配与日志。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 测试跑分 + 修复：执行 PR1~PR6 综合测试，`scripts/check.sh` 417 tests OK。实测 `feishu triggers` CLI（schedule/message/webhook 创建/list/log）、`feishu runs --patches`、schedule 边界、消息触发匹配均正常。发现并修复 `partner/runtime/agent/service.py` 缺少 `format_patches_text` 的 bug（`run_store.list_runs_text(task_id=...)` 导入失败），已补齐函数并重新跑绿。当前 alignment_score() = 82.0。
- 代码落地 PR6（条件触发器 + 统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook` 与 `condition`（keywords、chat_type、sender_id、webhook_path）；新增统一 `trigger_event_log` 表实现幂等去重与审计；`partner/ops/serve.py` 消息链路调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按子路径优先匹配 webhook 触发器；扩展 `partner/ops/triggers_cli.py` 支持 `--source`/`--condition-*` 与 `feishu triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 综合测试 + bug 修复：执行 `scripts/check.sh` 全绿（417 tests）；CLI 实测 `feishu triggers` schedule/message/webhook 三类增删查与 `triggers log`；实测 `feishu runs --patches --task-id` 显示意图补丁；实测 schedule 边界（daily/weekly/cron/once 合法与非法）与 message trigger 匹配/过滤。发现并修复 `partner/core/run_store.py` 引用不存在 `format_patches_text` 的 bug，在 `partner/runtime/agent/service.py` 中补完该函数；修复后 `runs --patches` 正常输出。重新跑 `scripts/check.sh` 全绿。
- 代码落地 PR6（条件触发器 + 统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook`、`condition` 含 keywords/chat_type/sender_id/webhook_path；新增统一 `trigger_event_log` 审计日志与幂等；`partner/ops/serve.py` 处理消息时调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按 `/webhook/<path>` 匹配触发器并 fallback 旧版通用队列；扩展 `partner/ops/triggers_cli.py` 支持 `--source`、`--condition-*` 与 `feishu triggers log`；新建 `tests/test_triggers_conditions.py`。更新 `partner/ops/aily.py`：`triggers` 82→88，`observability` 88→90，`alignment_score()` 81.5→82.0。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 测试实测与 bug 修复：执行 `bash scripts/check.sh`、triggers CLI 端到端、`feishu runs --patches --task-id`、schedule 边界与消息触发匹配实测。发现并修复 `partner/core/run_store.py` 引用不存在的 `format_patches_text` 导致 `feishu runs --patches --task-id` 崩溃；在 `partner/runtime/agent/service.py` 新增 `format_patches_text()`。修复后 `bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit（待提交）。
- 代码落地 PR6（条件触发器与统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook`、`condition`（keywords/chat_type/sender_id/webhook_path）、统一 `trigger_event_log` 表；`partner/ops/serve.py` 处理消息关键词触发；`partner/ops/webhook.py` 按子路径匹配 webhook 触发器并 fallback 通用 webhook；扩展 `partner/ops/triggers_cli.py` 支持 `--source`/`--condition-*` 与 `feishu triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 跑测并修复一个真实 bug：在 CLI `feishu runs --patches --task-id <id>` 实测时发现 `partner/core/run_store.py` 引用不存在的 `partner.runtime.agent.service.format_patches_text`，导致查询补丁任务崩溃；在 `partner/runtime/agent/service.py` 新增 `format_patches_text(task)` 函数，输出任务目标、意图补丁列表（动作/作者/合并状态/文本）与 observations。修复后 `bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `[待提交]`。
- 代码落地 PR6（条件触发器与统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 source=schedule/message/webhook，`condition` 支持 keywords/chat_type/sender_id/webhook_path；新增 `trigger_event_log` 统一审计与幂等；`partner/ops/serve.py` 消息入口调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按路径优先匹配 webhook 触发器；扩展 `feishu triggers log` 与 `--source`、`--condition-*` 选项；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 测试跑测：执行全量回归、triggers CLI 实测（schedule/message/webhook 创建与 list/log）、runs --patches 实测、schedule 边界测试、消息触发匹配实测；发现并修复 `partner/runtime/agent/service.py` 缺少 `format_patches_text` 导致 `feishu runs --patches` 崩溃的问题。修复后全量 417 tests OK。
- 代码落地 PR6（条件触发器与统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source=message/webhook`，条件匹配（关键词、聊天类型、发送者、webhook 路径），统一 `trigger_event_log` 表与幂等；`partner/ops/serve.py` 处理消息时调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按子路径优先匹配 webhook 触发器；扩展 `partner/ops/triggers_cli.py` 支持 `--source` / `--condition-*` 与 `log` 子命令；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0；新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 测试与 bug 修复：跑 PR1~PR6 综合测试时，发现 `partner/core/run_store.py` 引用了未定义的 `partner.runtime.agent.service.format_patches_text`，在 `partner/runtime/agent/service.py` 补上了 `format_patches_text(task)` 函数，确保 `feishu runs --patches --task-id <id>` 正常输出。补完后 `bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit（待命名）。
- 代码落地 PR6（条件触发器 + 统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source` 为 `message`/`webhook`，`condition` 支持 keywords/chat_type/sender_id/webhook_path；新增 `trigger_event_log` 幂等审计；`partner/ops/serve.py` 处理消息时匹配 message 触发器；`partner/ops/webhook.py` 按 `/webhook/<path>` 匹配 webhook 触发器，fallback 到旧版通用 webhook；扩展 `triggers_cli.py` 支持 `--source`、`--condition-*` 与 `feishu triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88，`observability` 88→90。新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16，`alignment_score()` = 82.0。已提交 commit `9f933d2`。
- 跑一轮综合测试过程中发现并修复 bug：`partner/core/run_store.py` 的 `list_runs_text(task_id=...)` 调用了不存在的 `partner.runtime.agent.service.format_patches_text`，导致 `feishu runs --patches --task-id` 崩溃。在 `partner/runtime/agent/service.py` 新增 `format_patches_text()` 函数，输出任务目标、intent_patches 明细与 observations。修复后重跑 `bash scripts/check.sh` 417 tests OK。
- 代码落地 PR6（条件触发器与统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 source=schedule/message/webhook、`condition`（keywords、chat_type、sender_id、webhook_path）、统一 `trigger_event_log` 表与幂等审计；`partner/ops/serve.py` 处理消息时调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按子路径优先匹配 webhook 触发器并 fallback 旧版通用 webhook；扩展 `partner/ops/triggers_cli.py` 的 `--source`、`--condition-*` 与 `log` 子命令；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90；新增 `tests/test_triggers_conditions.py`；更新 `AILY_ROADMAP_PRS.md` 加入 PR5/PR6。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 实测修复：CLI 测试发现 `partner/core/run_store.py` 引用了未定义的 `format_patches_text`（`feishu runs --patches --task-id` 路径），已在 `partner/runtime/agent/service.py` 新增该函数；测试验证 `feishu triggers` 三种 source、`schedule` 边界、`match_message_trigger` 过滤均正常。
- 代码落地 PR6（条件触发器 + 统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source=message/webhook/schedule`，`condition` 含 keywords/chat_type/sender_id/webhook_path；新建统一 `trigger_event_log` 幂等表；`partner/ops/serve.py` 收到消息时匹配关键词触发；`partner/ops/webhook.py` 按子路径匹配 webhook 触发器并 fallback；扩展 `partner/ops/triggers_cli.py` 支持 `--source`、`--condition-*` 与 `feishu triggers log`；新建 `tests/test_triggers_conditions.py`；更新 `partner/ops/aily.py`，`triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0；补齐 `AILY_ROADMAP_PRS.md` PR5/PR6。已提交 commit `9f933d2`。
- 综合测试轮次：完整 `bash scripts/check.sh` 417 tests OK + smoke 16/16；实测 triggers CLI 三种触发器创建/list/log、runs --patches 输出、schedule 边界、message trigger 匹配；修复 `partner/runtime/agent/service.py` 缺少 `format_patches_text` 导致的 `feishu runs --patches` 崩溃。当前 `alignment_score()` = 82.0。
- 代码落地 PR6（条件触发器与统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook` 与 `condition`（keywords/chat_type/sender_id/webhook_path）；新增统一 `trigger_event_log` 表实现幂等审计；`partner/ops/serve.py` 处理消息时调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按子路径优先匹配 webhook 触发器；扩展 `partner/ops/triggers_cli.py` 的 `--source`/`--condition-*` 与 `triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py`。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 综合测试跑测：重新跑 `bash scripts/check.sh` 全绿；实测 `feishu triggers add/list/log`、`feishu runs --patches`、`schedule` 边界、`match_message_trigger` 聊天类型/发送者/关键词过滤均正常。期间发现并修复 bug：`partner/core/run_store.py` 的 `list_runs_text(task_id=...)` 引用未定义的 `partner.runtime.agent.service.format_patches_text`，在 `service.py` 新增该函数后修复。
- 代码落地 PR6（条件触发器与统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook` 与 `condition`（keywords、chat_type、sender_id、webhook_path）；新增统一 `trigger_event_log` 表与 `{record,lookup,list}_trigger_events`；`partner/ops/serve.py` 处理消息时调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按子路径匹配 webhook 触发器后 fallback 旧版队列；扩展 `partner/ops/triggers_cli.py` 支持 `--source`/`--condition-*` 与 `feishu triggers log`；新增 `tests/test_triggers_conditions.py`。更新 `partner/ops/aily.py`：`triggers` 82→88，`observability` 88→90，`alignment_score()` 从 81.5→82.0。补齐 `AILY_ROADMAP_PRS.md` PR5/PR6。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 综合测试与 bugfix：跑通 `feishu triggers -- {list,add}` CLI 实测、schedule 边界测试、message/webhook 匹配测试、`feishu runs --patches --task-id` 实测。发现并修复 `partner/core/run_store.py` 引用未定义的 `format_patches_text` 导致的 `ImportError`，在 `partner/runtime/agent/service.py` 实现该函数。全量测试保持 417 tests OK。
- 代码落地 PR6（条件触发器与统一事件日志审计）：扩展 `partner/core/triggers.py` 支持 source=schedule/message/webhook，condition 支持 keywords/chat_type/sender_id/webhook_path；新增 `trigger_event_log` 表与幂等 `(source, external_id)` 审计；`partner/ops/serve.py` 消息处理链路接入 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 按 `/webhook/<path>` 优先匹配触发器；扩展 `partner/ops/triggers_cli.py` 支持 `--condition-*` 与 `feishu triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90，`alignment_score()` 从 81.5→82.0。新增 `tests/test_triggers_conditions.py`。已提交 commit `9f933d2`。
- 测试与修复：跑 PR1~PR6 综合测试时发现 `partner/core/run_store.py` 引用了未定义的 `format_patches_text`。在 `partner/runtime/agent/service.py` 补齐 `format_patches_text(task)`，使 `feishu runs --patches --task-id <id>` 正常输出。全量 `bash scripts/check.sh` 417 tests OK + smoke 16/16。
- 代码落地 PR6（条件触发器与统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source=schedule/message/webhook` 与 `condition`（keywords、chat_type、sender_id、webhook_path）；新增统一 `trigger_event_log` 表及幂等事件记录/查询；`partner/ops/serve.py` 处理消息时匹配关键词触发器；`partner/ops/webhook.py` 按子路径优先匹配 webhook 触发器；扩展 `feishu triggers` CLI 支持 `--source`、`--condition-*` 与 `log` 子命令；新增 `tests/test_triggers_conditions.py`。更新 `partner/ops/aily.py`：`triggers` 82→88，`observability` 88→90，`alignment_score()` 从 81.5→82.0。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 测试跑测与 bug 修复：按用户要求跑 PR1~PR6 实测，发现 `feishu runs --patches` 因 `partner/core/run_store.py` import 了不存在的 `format_patches_text` 而崩溃。已在 `partner/runtime/agent/service.py` 新增 `format_patches_text()` 格式化任务及其 intent patches。重新全量回归通过 417 tests OK + smoke 16/16。
- 代码落地 PR6（条件触发器与统一事件日志）：扩展 `partner/core/triggers.py` 支持 `source` 为 schedule/message/webhook；`condition` 支持 keywords、chat_type、sender_id、webhook_path；新增统一 `trigger_event_log` 表与 `{record,lookup,list}_trigger_events` 幂等审计；`partner/ops/serve.py` 处理消息时调用 `_maybe_run_message_triggers`；`partner/ops/webhook.py` 优先按子路径匹配 webhook 触发器；扩展 `partner/ops/triggers_cli.py` 支持 `--source`、`--condition-*` 与 `feishu triggers log`；更新 `partner/ops/aily.py` `triggers` 82→88、`observability` 88→90；新增 `tests/test_triggers_conditions.py` 覆盖匹配与日志。`bash scripts/check.sh` 417 tests OK + smoke 16/16。已提交 commit `9f933d2`。
- 综合测试跑测：运行 `scripts/check.sh` 全绿；手动验证 `feishu triggers -- add/list/log`、`feishu runs --patches --task-id`、schedule 边界计算、消息触发匹配均正常。发现并修复一个真实 bug：`partner/core/run_store.py` 引用 `partner.runtime.agent.service.format_patches_text` 但该函数不存在，新增 `format_patches_text()` 后重新全绿。
