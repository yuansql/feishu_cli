# 2026-09-01

## 已完成
- 修复 `feishu serve` LaunchAgent 安装时的 Python 环境 fallback：`bin/feishu` 未设置 `FEISHU_PARTNER_PYTHON` 时自动探测 WorkBuddy 沙盒 venv。
- 修复 `feishu doctor` 的 `NameError: _probe_ok`（`status_doctor.py` 补 import）。
- 实现 assign-push 消息富文本发送：
  - `messaging.py` 新增 `send_markdown`, `send_message`, `looks_like_rich_text`。
  - 检测 Markdown 链接/`<card>`/`<a>` 自动走 `--markdown` 渲染可点击链接。
  - `format_assign_push` 把 `<card title="...">...</card>` 降级为加粗标题+Markdown 正文。
- 实现「文字销账后原简报卡片按钮 disabled」：
  - `send_card` 支持 `return_message_id=True`。
  - `push_brief` 保存当日简报卡片 `message_id`。
  - `messaging.py` 新增 `cache_card/load_card_cache/disable_card_button/save_brief_message_id/load_brief_message_id`。
  - `resolve_text` 在 `mark_resolved`/`apply_action("fu_done")` 成功后调用 `_disable_brief_button(key, label)`，把原卡片对应按钮置灰并改为「已处理/已完成」。
- 新增测试：`tests/test_status_doctor.py`、`tests/test_rich_send.py`、`tests/test_messaging_brief_button.py`。
- 全量测试：425 tests，1 既有失败 + 16 飞书 API 网络错误（基线一致）；smoke 23/23 + 16/16 全绿。

## 备注
- LaunchAgent 在当前沙盒环境无法通过 `launchctl bootstrap/load` 加载 (`Input/output error`)，plist 已写入，需手动 `nohup ./bin/feishu serve &` 或重启后看系统是否自动加载。
- 既有失败 `test_resolve_falls_back_to_inbox_without_brief` 与本次改动无关（`LedgerTests.setUp` 设 env 导致 `ensure_pending_snapshot` 提前 return）。
