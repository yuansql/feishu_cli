# 2026-08-21

## feishu_cli vs 飞书 Aily 差距对比（全力模式）
- 路径：`/Users/wumm/学习/@Agent/feishu_cli/`
- 架构：Python 标准库 + lark-cli + LangGraph v2；分层 core/routing/runtime/office/compose/ops
- 自评 ~70/100（14 项加权），目标 ≥90/100
- 关键差距按杠杆重排：
  - 🔴 async_sandbox (10) / knowledge RAG (9) / agent_runtime 持久化 (14) ≈ +20
  - 🟡 multi_agent / workflow / artifacts / memory ≈ +12
  - 🟢 governance / publish / mcp / observability 已过线
- 核心限制（用户的判断）：
  - 90/100 单靠代码补不到，剩 ~10 分是云沙箱 + 模型规划质量
  - 务实目标改 85/100，差额写进文档承认，不要无限堆代码假装对齐
- 用户为什么要折腾 yukiapi：本地默认 `Kimi-K2.6` + 启发式，长任务规划质量不够，想换更强脑子

## yukiapi 配置（Aily 控制台「编辑模型」）诊断
- 截图位置：`~/.workbuddy-ai/clipboard-images/clipboard-2026-08-21T02-44-35-259Z-03ca693a.png`
- 截图是 Aily 控制台，不是 feishu_cli
- 头号嫌犯：`gpt-5.5` 几乎肯定错（OpenAI 家族无此命名；去 yukiapi 文档拿真实 ID）
- 二号嫌犯：「自定义协议」不该勾（URL 是 `/v1/chat/completions` 标准 OpenAI 兼容）
- 三号嫌犯：图像输入 + 推理式同时开（很多模型不真支持 vision/reasoning）
- 调试顺序：全关 → 改模型名 → 200 OK → 一个个开
- 绕开 Aily UI：`feishu agent init` + brain=openai + OPENAI_BASE_URL=... 或环境变量
- 这是 feishu_cli 存在意义：直接吃 OpenAI 兼容 schema，没那五个杂项开关

## yukiapi 配置落地（实测）
- 旧 agent.json：`~/.feishu-partner/agent.json` 原本指向 delta-router.yangtzeailab.com + Kimi-K2.6 + 3 把 key
- 已备份旧文件：`~/.feishu-partner/agent.json.bak.<时间戳>`
- 已写入新配置（brain=openai + yukiapi）：
  - openai_base_url: https://yukiapi.com/v1
  - openai_model: gpt-image-2  ← 这是该 key 下唯一可用模型；`gpt-5.5` 不存在（model_not_found）
  - yukiapi `/v1/models` 只挂 `gpt-image-2`（类型 image-generation，但 chat 接口可用）
- 注意：`compose/llm.py` 润色路径只吃 Hermes，yukiapi 只影响 runtime/agent 决策脑
- 改配置时踩的坑：Write 工具一度误写到项目 `.workbuddy-ai/agent.json`（含 key），已 `rm` 掉；正确落点是 `~/.feishu-partner/agent.json`（仓库外私有）

## yukiapi gpt-5.6-terra 接入 WorkBuddy 自定义模型（11:13）
- 用户用 Aily 控制台"编辑模型"配置卡住，改走 `~/.workbuddy-ai/models.json` 直接编辑
- 截图：`~/.workbuddy-ai/clipboard-images/clipboard-2026-08-21T03-13-42-224Z-06050755.png`
- 已知 `~/.workbuddy-ai/models.json` schema（参照 Kimi-K2.6 推断）：
  - `id / name / vendor / apiKey / url` 五件套
  - `supportsToolCall / supportsImages / supportsReasoning / useCustomProtocol / maxInputTokens / maxOutputTokens`
  - `reasoning.{supportedEfforts: [low|medium|high|xhigh|max], canDisableThinking: bool}`
- UI 的 bug：勾完"推理模式/允许关闭思考/五个思考强度"后**不写 `reasoning` 块** → 模型面板掉档（拿不到完整 capability）
- 修复：在 JSON 里手补 `reasoning` 块，schema 与 Kimi-K2.6 对齐
- 验证：`python3 -c 'import json; json.load(open(...))'` 通过；3 个模型都还在
- 备查：`/v1/models` 返回 200，仅 `gpt-5.6-terra`（OpenAI 兼容）
- 后续策略：与本机 Hermes + 飞书 CLI 通道对齐——gpt-5.6-terra 可作本地模型后端，飞书 CLI 出授权，Hermes 出智能

## 项目约定（备忘）
- 仓内零密钥；身份 `~/.feishu-partner/config.json`（不进 git）
- 不对接 Aily 接口；Aily 是对标清单
- 单聊走隔离 Hermes `feishupartner`，禁 `--yolo`
